const multiplicaNumeros = (numeroUm: number, numeroDois: number): number => {
    return numeroUm * numeroDois;
}

console.log(multiplicaNumeros(262, 8.6));

const retornaFrase = (nome: string): string => {
    return 'Olá ' + nome;
}

console.log(retornaFrase('Pedro'));